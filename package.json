{
  "name": "styx-river",
  "version": "0.0.0",
  "scripts": {
    "dev": "standard && next",
    "build": "standard && next build",
    "start": "next start",
    "lint": "standard",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "AGPL-3.0",
  "dependencies": {
    "isomorphic-fetch": "^2.2.1",
    "next": "^2.4.0",
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "standard": "^10.0.2"
  }
}
